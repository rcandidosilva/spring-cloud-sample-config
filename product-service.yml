server:
  port: ${PORT:8082}

security:
  oauth2:
    resource:
      userInfoUri: http://localhost:9999/uaa/user

---

spring:
  profiles: docker

security:
  oauth2:
    resource:
      userInfoUri: http://security:9999/uaa/user