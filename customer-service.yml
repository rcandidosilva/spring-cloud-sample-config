server:
  port: ${PORT:8082}

spring:
  jpa:
    hibernate:
      ddl-auto: create

security:
  oauth2:
    resource:
      userInfoUri: http://localhost:9999/uaa/user

---

spring:
  profiles: docker

security:
  oauth2:
    resource:
      userInfoUri: http://security-service:9999/uaa/user
