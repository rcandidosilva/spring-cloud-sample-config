server:
  port: ${PORT:8080}

endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true
  health:
    sensitive: false

zuul:
  routes:
    gft:
      path: /product/**
      serviceId: PRODUCT-SERVICE
    security:
      path: /security/**
      serviceId: SECURITY-SERVICE